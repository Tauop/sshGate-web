---
users_keys_dir: keys/users
targets_keys_dir: keys/targets

database:
  production:
    adapter: sqlite3
    database: sshgate.sqlite
  development:
    adapter: sqlite3
    database: dev_sshgate.sqlite
  test:
    adapter: sqlite3
    database: test_sshgate.sqlite
